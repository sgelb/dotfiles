term screen-256color
altscreen on
caption always "%{= kW} %-w%{=b wk} %n %t %{-}%+w %=%{= dd}"
shell zsh

screen -t "zsh" 0 /bin/zsh
screen -t "zsh" 1 /bin/zsh

# unbind some keys
bind .
bind ^\
bind \\
bind K
bind ^s
